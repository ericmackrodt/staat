{
  "name": "staat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:core": "cd packages/core && yarn build",
    "build:merge": "cd packages/merge && yarn build",
    "build:react": "cd packages/react && yarn build",
    "build:example": "cd packages/example && yarn build",
    "build": "yarn build:core && yarn build:react",
    "example": "yarn build && cd packages/example && yarn start",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^23.3.10",
    "jest": "^23.6.0",
    "lerna": "^3.6.0",
    "ts-jest": "^23.10.5",
    "typescript": "^3.2.2"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ]
}
